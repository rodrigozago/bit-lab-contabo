# On Air não precisa de secrets — o SSO é gate puro no nginx da VPS (ver README).
# Tudo abaixo é opcional (defaults no código / docker-compose.yml):
#
# PORT=3001            # porta da API
# DB_DIR=./.data       # diretório do SQLite (on-air.db)
# MEDIA_DIR=./.data/media  # fotos dos artistas
